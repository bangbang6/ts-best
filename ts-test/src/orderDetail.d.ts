export default class OrderDetail {
    orderDetailId: number;
    productname: string;
    price_: number;
    count: number;
    constructor(orderDetailId: number, productname: string, price_: number, count: number);
}
